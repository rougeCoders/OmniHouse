import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    container :{
        padding:'10%',
        marginTop:'50%',
        justifyContent: 'center',
    },
    signupButton:{
        marginTop:10,
    }
});