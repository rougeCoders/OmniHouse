import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    buttonContainer: {
      alignItems: 'center',
    },

    imageIcon: {
      borderRadius:10,
      width: 80,
      height: 80
    }
});