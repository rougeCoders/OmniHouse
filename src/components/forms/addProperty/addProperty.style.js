import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    container :{
        padding:'10%',
        marginTop:'10%',
        justifyContent: 'center',
    },
    buttonBottom:{
        bottom: 0,
    }
});